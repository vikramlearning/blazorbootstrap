<Toasts class="p-3" Messages="messages" Placement="ToastsPlacement.TopRight" AutoHide="false" />

<Button Color="ButtonColor.Primary" @onclick="OnShowToastClick">Show Toast</Button>

@code {
    List<ToastMessage> messages = new List<ToastMessage>();

    private void OnShowToastClick()
    {
        messages?.Add(new ToastMessage
            {
                Type = ToastType.Warning,
                IconName = IconName.Alarm,
                Title = "Blazor Bootstrap",
                HelpText = $"{DateTime.Now}",
                Message = $"Hello, world! This is a toast message. DateTime: {DateTime.Now}",
            });
    }
}
