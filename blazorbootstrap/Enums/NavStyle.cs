namespace BlazorBootstrap;

public enum NavStyle
{
    /// <summary>
    /// Takes the basic nav and adds the .nav-tabs class to generate a tabbed interface.
    /// </summary>
    Tabs,

    /// <summary>
    /// Takes the basic nav and adds the .nav-pills class to generate a tabbed interface.
    /// </summary>
    Pills
}