@using BlazorBootstrap
@page "/buttons"

<h4>Buttons</h4>
<p>
    <Button Color="Color.Primary"> Primary </Button>
    <Button Color="Color.Secondary"> Secondary </Button>
    <Button Color="Color.Success"> Success </Button>
    <Button Color="Color.Danger"> Danger </Button>
    <Button Color="Color.Warning"> Warning </Button>
    <Button Color="Color.Info"> Info </Button>
    <Button Color="Color.Light"> Light </Button>
    <Button Color="Color.Dark"> Dark </Button>
    <Button Color="Color.Link"> Link </Button>
</p>

<h4>Button tags</h4>
<p>
    <Button Type="ButtonType.Link" Color="Color.Primary" To="#"> Link </Button>
    <Button Type="ButtonType.Submit" Color="Color.Primary" To="#"> Button </Button>
</p>

<h4>Outline buttons</h4>
<p>
    <Button Color="Color.Primary" Outline="true"> Primary </Button>
    <Button Color="Color.Secondary" Outline="true"> Secondary </Button>
    <Button Color="Color.Success" Outline="true"> Success </Button>
    <Button Color="Color.Danger" Outline="true"> Danger </Button>
    <Button Color="Color.Warning" Outline="true"> Warning </Button>
    <Button Color="Color.Info" Outline="true"> Info </Button>
    <Button Color="Color.Dark" Outline="true"> Dark </Button>
</p>

<h4>Sizes</h4>
<p>
    <Button Color="Color.Primary" Size="Size.Large"> Large button </Button>
    <Button Color="Color.Secondary" Size="Size.Large"> Large button </Button>
</p>
<p>
    <Button Color="Color.Primary" Size="Size.Small"> Small button </Button>
    <Button Color="Color.Secondary" Size="Size.Small"> Small button </Button>
</p>

<h4>Disabled state</h4>
<p>
    <Button Color="Color.Primary" Size="Size.Large" Disabled="true"> Large button </Button>
    <Button Color="Color.Secondary" Size="Size.Large" Disabled="true"> Large button </Button>
</p>
<p>
    <Button Type="ButtonType.Link" Color="Color.Primary" Size="Size.Large" Disabled="true"> Primary link </Button>
    <Button Type="ButtonType.Link" Color="Color.Secondary" Size="Size.Large" Disabled="true"> Link </Button>
</p>

<h4>Block buttons</h4>
<div class="d-grid gap-2">
    <Button Color="Color.Primary"> Button </Button>
    <Button Color="Color.Primary"> Button </Button>
</div>

<div class="d-grid gap-2 d-md-block mt-2">
    <Button Color="Color.Primary"> Button </Button>
    <Button Color="Color.Primary"> Button </Button>
</div>

<div class="d-grid gap-2 col-6 mx-auto mt-2">
    <Button Color="Color.Primary"> Button </Button>
    <Button Color="Color.Primary"> Button </Button>
</div>

<div class="d-grid gap-2 d-md-flex justify-content-md-end mt-2">
    <Button Color="Color.Primary"> Button </Button>
    <Button Color="Color.Primary"> Button </Button>
</div>

<h4>Toggle states</h4>
<p>
    <Button Color="Color.Primary"> Toggle button </Button>
    <Button Color="Color.Primary" Active="true"> Active toggle button </Button>
    <Button Color="Color.Primary" Disabled="true"> Disabled toggle button </Button>
</p>
<p>
    <Button Type="ButtonType.Link" Color="Color.Primary" To="#"> Toggle button </Button>
    <Button Type="ButtonType.Link" Color="Color.Primary" To="#" Active="true"> Active toggle button </Button>
    <Button Type="ButtonType.Link" Color="Color.Primary" To="#" Disabled="true"> Disabled toggle button </Button>
</p>

<h4>Loading</h4>
<p>
    <Button Color="Color.Primary" Loading="true" />
    <Button Color="Color.Primary" Loading="true" LoadingText="Saving..." />
    <Button Color="Color.Primary" Loading="true">
        <LoadingTemplate>
            <span class="spinner-grow spinner-grow-sm" role="status" aria-hidden="true"></span>
            Loading...
        </LoadingTemplate>
    </Button>
</p>

<h4>Button with Tooltip</h4>
<p>
    <Button Color="Color.Primary" TooltipText="Tooltip text" TooltipPlacement="TooltipPlacement.Top"> Tooltip Top </Button>

    <Button Color="Color.Primary" TooltipText="Tooltip text" TooltipPlacement="TooltipPlacement.Right"> Tooltip Right </Button>

    <Button Color="Color.Primary" TooltipText="Tooltip text" TooltipPlacement="TooltipPlacement.Bottom"> Tooltip Bottom </Button>

    <Button Color="Color.Primary" TooltipText="Tooltip text" TooltipPlacement="TooltipPlacement.Left"> Tooltip Left </Button>
</p>

<h4>Native Click</h4>
<p>
    <Button Color="Color.Primary" @onclick="OnClick"> Click Event </Button>
</p>
<p>
    <Button Color="Color.Primary" @ondblclick="OnDoubleClick"> Double Click Event </Button>
</p>
<p>
    <Button Color="Color.Primary" @onclick="((args) => OnClickWithArgs(args, message))"> Click + Args </Button>
</p>

@code{

    public string message = "Test message";

    protected void OnClick(EventArgs args)
    {
        Console.WriteLine("click event");
    }

    protected void OnDoubleClick(EventArgs args)
    {
        Console.WriteLine("double click event");
    }

    protected void OnClickWithArgs(EventArgs args, string message)
    {
        Console.WriteLine($"message: {message}");
    }

}